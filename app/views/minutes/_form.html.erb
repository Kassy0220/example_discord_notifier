<%= form_with(model: [course, minute]) do |form| %>
  <% if minute.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(minute.errors.count, "error") %> prohibited this minute from being saved:</h2>

      <ul>
        <% minute.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :meeting_date, style: "display: block" %>
    <%= form.date_field :meeting_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
